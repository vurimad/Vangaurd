# Premake build policies

Shared project helpers, platform policies, and configuration policies belong in
this directory as the workspace grows. The root `premake5.lua` remains the
single workspace entry point.

